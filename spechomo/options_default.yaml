classifiers:
    RFR:  # Random Forest
        n_trees: 50
        max_depth: 5
        max_features: 'auto' # works well according to out tests but 'log2' is also good for RFR regression

    trainspec_filtering:
        max_distance: '80%'
        max_angle: 5
